package com.twitterclone.notification.domain.valueobjects

/**
 * `StringContent` is a concrete class that extends the [Content] base class.
 * It represents a content that is of string type.
 *
 * @param value The actual string content.
 */
class StringContent(value: String) : Content(value)
